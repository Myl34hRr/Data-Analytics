/*
a) the products table holds the northwind items 
b) categories table holds the type/categories of the items in northwind sells 
SELECT * FROM employees;
a) the employee with the name like a bird is peacock margaret 
SELECT * FROM products 
a) the quer returns 77 records, by going to the tool bar at the top select query, there is a limit rows option click that and choose limit to 10 rows 
SELECT * FROM categories 
c) the categoryid # for seafood is 8
SELECT orderid, orderdate, shipname, shipcountry 
FROM orders 
LIMIT 50; 

